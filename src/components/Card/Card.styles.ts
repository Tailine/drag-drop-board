import { styled } from '@/config'

export const Wrapper = styled('div', {
  height: 'auto',
  backgroundColor: '$blue900',
  width: '100%',
  borderRadius: '$rounded',
  padding: '$sm',
  margin: '$sm 0',
  cursor: 'pointer'
})
